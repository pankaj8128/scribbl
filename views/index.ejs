<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scribbl - Draw, Guess & Have Fun!</title>
    <meta name="description"
        content="Scribbl is a free online multiplayer drawing and guessing game. Draw, guess and have fun with friends!">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:wght@400;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/indexcss.css">
</head>

<body>
    <div class="background-shapes">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
        <div class="shape shape-3"></div>
        <div class="shape shape-4"></div>
        <div class="shape shape-5"></div>
        <div class="shape shape-6"></div>
    </div>

    <div class="page-wrapper">
        <div class="logo-section">
            <h1 class="logo">
                <span class="letter-s">S</span>
                <span class="letter-c">c</span>
                <span class="letter-r">r</span>
                <span class="letter-i">i</span>
                <span class="letter-b">b</span>
                <span class="letter-b">b</span>
                <span class="letter-l">l</span>
            </h1>
            <p class="tagline">Draw, Guess & Have Fun!</p>
        </div>

        <div class="container">
            <div class="card unified-card">
                <div class="card-header">
                    <h2>Play Scribbl!</h2>
                </div>
                <p class="card-desc">Enter your username and click Play to draw & guess with random players, or play with friends below!</p>
                <form id="scribbl-lobby-form" action="/play-random" method="post">
                    <!-- Shared Username Input -->
                    <div class="input-group">
                        <input type="text" id="lobby-username" name="username" placeholder="Enter your name..." required>
                    </div>

                    <!-- Big Play Button for Public Match (Play with Randoms) -->
                    <button type="submit" id="btn-play-random" class="btn btn-play-random">
                        <span class="btn-text">Play! (Random Match)</span>
                    </button>

                    <div class="card-section-divider">
                        <div class="divider-line"></div>
                        <span class="divider-text">OR PLAY WITH FRIENDS</span>
                        <div class="divider-line"></div>
                    </div>

                    <!-- Actions Grid -->
                    <div class="actions-grid">
                        <button type="button" id="btn-create-private" class="btn btn-create-private">
                            <span class="btn-text">Create Private Room</span>
                        </button>
                    </div>

                    <!-- Join Section -->
                    <div class="join-private-section">
                        <div class="input-group">
                            <input type="text" id="lobby-room-code" name="roomCode" placeholder="Enter Room Code..." value="<%= locals.prefillRoom || '' %>">
                        </div>
                        <button type="button" id="btn-join-private" class="btn btn-join-private">
                            <span class="btn-text">Join Room</span>
                        </button>
                    </div>
                </form>
            </div>
        </div>

        <footer class="footer">
            <p>Made with ❤️ | Scribbl Clone by <a href="https://panky.qzz.io" target="_blank">Pankaj
                    Jagadale</a></p>
        </footer>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', () => {
            const form = document.getElementById('scribbl-lobby-form');
            const usernameInput = document.getElementById('lobby-username');
            const roomCodeInput = document.getElementById('lobby-room-code');
            const btnPlayRandom = document.getElementById('btn-play-random');
            const btnCreatePrivate = document.getElementById('btn-create-private');
            const btnJoinPrivate = document.getElementById('btn-join-private');

            // Focus username input
            usernameInput.focus();

            // Create Private Room action
            btnCreatePrivate.addEventListener('click', () => {
                if (!usernameInput.value.trim()) {
                    usernameInput.reportValidity();
                    return;
                }
                form.action = '/create-room';
                form.submit();
            });

            // Join Private Room action
            btnJoinPrivate.addEventListener('click', () => {
                if (!usernameInput.value.trim()) {
                    usernameInput.reportValidity();
                    return;
                }
                if (!roomCodeInput.value.trim()) {
                    roomCodeInput.reportValidity();
                    return;
                }
                form.action = '/join-room';
                form.submit();
            });

            // Smart Enter key handling
            usernameInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') {
                    e.preventDefault();
                    if (roomCodeInput.value.trim()) {
                        btnJoinPrivate.click();
                    } else {
                        btnPlayRandom.click();
                    }
                }
            });

            roomCodeInput.addEventListener('keypress', (e) => {
                if (e.key === 'Enter') {
                    e.preventDefault();
                    btnJoinPrivate.click();
                }
            });

            // If prefilled room, scroll to join room inputs
            <% if (locals.prefillRoom) { %>
                usernameInput.focus();
                document.querySelector('.join-private-section').scrollIntoView({ behavior: 'smooth' });
            <% } %>
        });
    </script>
</body>

</html>
